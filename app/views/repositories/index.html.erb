<script>
    let a = document.getElementById("maincon");
    a.setAttribute("style","padding-top: 5%");
</script>

<p id="notice"><%= notice %></p>

<h1>Repositories</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Pemilik</th>
      <th>Akses</th>
      <th>Katagori</th>
      <th>Namafile</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @repositories.each do |repository| %>
      <tr>
        <td><%= repository.code %></td>
        <td><%= repository.pemilik %></td>
        <td><%= repository.akses %></td>
        <td><%= repository.katagori %></td>
        <td><%= repository.namafile %></td>
        <td><%= link_to 'Show', repository %></td>
        <td><%= link_to 'Edit', edit_repository_path(repository) %></td>
        <td><%= link_to 'Destroy', repository, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->
<div class="jumbotron" style="background-color: inherit;">
  <div class="row">

      <% @repositories.each do |repository| %>
        <br>
      <div class="col-sm-3">
        <a>
          <div class="card">
            <div class="card-header text-center">
              <%= link_to repository.namafile,repository %>
            </div>
            <div class="card-body">
              <%= repository.pemilik %>
            </div>
            <div class="card-footer text-right">
              <%= repository.katagori %>
              <div class="text-right">
                <a class="btn btn-link text-dark" href="<%= edit_repository_path(repository) %>"> Buka
                </a>
                  <%= link_to 'Destroy', repository, method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
            </div>
          </div>
        </a>
      </div>


      <% end %>
  </div>
</div>

<br>
